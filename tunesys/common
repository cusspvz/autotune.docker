#!/bin/bash

setp () {
    echo -ne "Setting up $1 to $2 ..."
    _PATH=$PROC/$1

    # Don't set anything if path doesn't exist
    [ -f $_PATH ] && {
        echo $2 > $_PATH
        echo "Ok"
    } || {
        echo "Failed"
    }

}

throw () {
    echo "Error: $2"
    exit $1
}
